mainmodule main {
    readonly CProxy_Main mainProxy;
	extern module wp_utils;

    mainchare Main {
        entry Main(CkArgMsg *m);
        entry void checkIn();
        entry void RecvFile(FDataMsg *fd);
    };
};

