mainmodule wp {
	readonly CProxy_Main mainProxy;

	mainchare Main {
		entry Main(CkArgMsg *m);
		entry void done();
	};

	message FNameMsg {
		char fname[];
	};

	group FileReader {
		entry FileReader();
		entry void GetFilename(FNameMsg *msg);
	};
};

